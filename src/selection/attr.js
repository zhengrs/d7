import "selection";


// Element.getAttribute(name)
d7_selectionPrototype.attr = function(name, value){
  
  var node;
  
  if(arguments.length < 2){
    if(node = this.node()){
      return node.
    }
  }
  else{
    if(this.node
    
    
  }
  
  
};
